name: plugin-ci

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: plugin-ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    uses: SignalK/signalk-server/.github/workflows/plugin-ci.yml@ac368c548fba923db0acf8a314cbdc6552b2b8cf
    with:
      format-check-command: npm run format:check
      coverage-command: npm run test:coverage
      node-versions: '["22", "24"]'
      enable-armv7: false
      enable-signalk-integration: true
      signalk-server-versions: '["2.25.0", "latest"]'
