# Build artifacts
node_modules/
dist/
public/
coverage/
*.tsbuildinfo
.cache/
.tmp/

# Environment and secrets (never commit)
.env
.env.*
!.env.example
*.local
secrets.json
credentials.json
*.pem
*.key
*.p12
*.pfx
auth.json
.npmrc

# Signal K runtime data (if the user happens to symlink the repo
# next to plugin-config-data, this prevents the JSON config file
# that holds the OpenRouter API key from being staged by mistake)
plugin-config-data/
signalk-openrouter-companion.json
reports.jsonl

# Editor backups and OS files
.DS_Store
Thumbs.db
*~
*.swp
*.swo
.vscode/
.idea/

# npm pack output
*.tgz

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Tooling state
.last-verified-sha
.claude/scheduled_tasks.lock
